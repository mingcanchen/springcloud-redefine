spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mingcanchen/others.git
          #username:
          #password:
          search-paths: sc-redefine-config
  application:
    name: ch12-8-config-server
server:
    port: 9090